{
  "name": "nalae-core",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspace nalae-core tsc",
    "clean": "rm -rf ./dist",
    "start": "yarn workspace nalae-core clean && yarn workspace nalae-core build && node ./dist/index.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^13.7.1"
  }
}
